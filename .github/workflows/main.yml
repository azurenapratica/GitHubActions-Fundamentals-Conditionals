name: Treinamento

on: 
 push:
  branches: 
   - main
  paths-ignore: 
   - /.github/worflows/main.yml
 workflow_dispatch:

jobs:
 build:
   runs-on: ubuntu-latest
   steps:
    - name: Task 1 Exemplo
      run: echo 'Task 1 Exemplo'
